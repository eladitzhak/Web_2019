			// case 'DELETE':
		// 	if (urlObj.path === '/deleteOrder') {
		// 		console.log('/deleteOrder Called!');
		// 		let i = 0;
		// 		for ( i = 0; i < data.length; i++) {
		// 			if (data[i] && data[i].hasOwnProperty(key)  {
		// 				retiem data[i][key]
		// 			}
		// 		}


		// 	}